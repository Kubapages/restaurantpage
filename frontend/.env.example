# Skopiuj ten plik jako .env.local
# Nie ustawiaj VITE_API_URL - wtedy Vite proxy automatycznie prześle
# żądania z /api/* na localhost:8000 (działa lokalnie bez Dockera)
# VITE_API_URL=http://localhost:8000
